# API Contracts Repository

This repository contains API definitions and contracts, including Protobuf and OpenAPI schemas, for various services.

## Structure

- `proto/`: Contains Protobuf definitions for each service.
- `openapi/`: Contains OpenAPI schemas and documents for each service.

## Usage

### Generating Protobuf Code

To generate Protobuf code, run the following command:

```sh
# TODO
```
